# online-meeting
## to start the app 

1. clone  repo 
```sh
  git clone this is link 
```
2.run the server
-grt into server folder
  ```sh
  cd server
   ```
-install dependancy
  ```sh
    npm install 
  ```
-install finally run server
  ```sh
    nodemon server.js 
    ```
rt 
    ```
